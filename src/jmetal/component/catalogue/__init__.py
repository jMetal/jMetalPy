"""Catalogue of reusable algorithm components.

`common/` holds components shared by every algorithm family (solution creation,
evaluation, termination); `ea/` holds the ones specific to evolutionary algorithms
(selection, variation, replacement).
"""
