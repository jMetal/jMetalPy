"""Component-based algorithm core.

This package mirrors jMetal Java's `jmetal-component` module: algorithms are assembled
from small, independently testable components (solution creation, evaluation,
termination, selection, variation, replacement) instead of being defined by
subclassing. It lives alongside `jmetal.algorithm`, which keeps working unchanged.
"""
